# Development dependencies for DataMind Backend
# Install: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1          # already in main, but needed for TestClient
aiosqlite==0.20.0      # async SQLite for unit tests

# Code quality
ruff==0.8.4
